--掉落系统
-- 随机掉落 幸运掉落
-- 固定掉落
-- 任务掉落
-- 时间掉落
FallSystem = {}
FallSystem.Type = {"固定数值","范围随机","等级","其他"}
FallSystem.Group = {
    [1] = {
        Type = 0
    }
}
function FallSystem.Random()
    -- statements
end
return FallSystem